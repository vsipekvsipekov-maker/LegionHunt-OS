import { NextRequest,NextResponse } from "next/server";import{db,ensureCrmSchema}from"@/lib/db";
export async function POST(r:NextRequest){try{await ensureCrmSchema();const b=await r.json();const x=await db.query(`INSERT INTO legionhunt_academy_modules(course_id,title,description,position) VALUES($1,$2,$3,COALESCE((SELECT MAX(position)+1 FROM legionhunt_academy_modules WHERE course_id=$1),1)) RETURNING id::int,title,description,position`,[Number(b.courseId),String(b.title||'Новый модуль').trim()||'Новый модуль',String(b.description||'')]);return NextResponse.json({module:x.rows[0]},{status:201})}catch(e){console.error(e);return NextResponse.json({error:'Не удалось создать модуль.'},{status:500})}}
